
<div class="hero min-h-screen bg-base-200">
  <div class="hero-content flex-col lg:flex-row-reverse">
    <img src={{artPiece.image}} class="max-w-sm rounded-lg shadow-2xl p-3" />
    <div class="p-3">
      <h1 class="text-5xl font-bold">{{artPiece.title}}</h1>
      <p class="py-6" style="font-family: 'Poppins', sans-serif;">{{artPiece.description}}</p>
      <p class="py-6" style="font-family: 'Poppins', sans-serif;">${{artPiece.price}}</p>
      {{#if artPiece.is_for_sale}}
      <button class="btn btn-accent">BUY NOW</button>
      {{else}}
      <button class="btn" disabled="disabled">NOT FOR SALE</button>
      {{/if}}
    </div>
  </div>
</div>




{{!-- <div class="flex flex-row justify-around w-3/4 mx-auto">
    <div class="card w-96 bg-base-100 shadow-xl">
        <figure><img src={{artPiece.image}} alt="Art" /></figure>
        <div class="card-body">
            <h2 class="card-title" style="font-family: 'Poppins', sans-serif;">{{artPiece.title}}</h2>
            <div class="card-actions justify-end">
            </div>
        </div>
    </div>
    <div class="card w-96 bg-base-100 shadow-xl">
        <div class="card-body">
            <h2 class="card-title" style="font-family: 'Poppins', sans-serif;">{{{artPiece.collection.title}}}</h2>
            <p style="font-family: 'Poppins', sans-serif;">Description: {{artPiece.description}}</p>
            <h3 style="font-family: 'Poppins', sans-serif;">Price: ${{artPiece.price}}.00</h3>
            <div class="card-actions justify-end">
                <button class="btn btn-accent">BUY NOW</button>
            </div>
        </div>
    </div>

</div> --}}


