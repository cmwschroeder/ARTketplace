
    <div id="hero-image">
        <h2></h2>
        <a></a>
    </div>
     <div class="w-11/12 mx-3 mt-10 mb-3 flex flex-row justify-around">
        <button class="btn btn-primary m-2 w-1/3" id="Sort-button">Sort By</button>
        <button class="btn btn-secondary m-2 w-1/3" id="Filter-button">Filter</button>
        <div class="search-box">
            <input type="text" placeholder="Search Here..." class="search-input">
            <a href="#" class="search-btn"><button class="search-btn btn-accent m-2 w-1/3" id="Search-button">Search</button></a>
            <i class="fa fa-search"></i>
        </div>
        
    </div>
    
    <div class="w-full h-full hidden flex-row flex-wrap justify-around" id="art-section">
        {{#each artPieces as |artPiece|}}
        <div class="card w-96 bg-base-100 shadow-xl m-3">
            <figure><img class="h-48" src={{artPiece.image}} alt="Art" /></figure>
            <div class="card-body">
                <div class="w-full text-center">
                    <p class="text-3xl">${{artPiece.price}}</p>
                </div>
                <h2 class="card-title">{{artPiece.title}}</h2>
                <p>{{artPiece.description}}</p>
                <div class="card-actions justify-end">
                    <button class="btn btn-secondary">Edit</button>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    
    <footer>
        <div id="footer-info"></div>
        <div id="footer-links">
            <ul></ul>
            <ul></ul>
            <ul></ul>
        </div>
    </footer>